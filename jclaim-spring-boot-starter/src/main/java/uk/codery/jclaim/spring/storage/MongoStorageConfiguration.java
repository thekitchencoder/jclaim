package uk.codery.jclaim.spring.storage;

import org.springframework.context.annotation.Configuration;

/**
 * Mongo storage wiring. Intentionally empty in this commit — populated in
 * a subsequent commit that adds the Mongo classpath + bean conditionals.
 */
@Configuration(proxyBeanMethods = false)
public class MongoStorageConfiguration {
}
